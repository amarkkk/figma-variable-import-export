{
  "name": "figma-variable-import-export",
  "version": "1.0.0",
  "description": "Export Figma variables to CSV/JSON for bulk editing in spreadsheets, then import back",
  "author": "Mark Andrassy",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.119.0",
    "typescript": "^5.9.3"
  }
}
